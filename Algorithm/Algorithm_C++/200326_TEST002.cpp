#include <iostream>
#include <cstring>
#include <math.h>
#include "string"
using namespace std;

class Node{
    int data;
    Node left;
    Node right;
};

int main() {
    int n, m;
    int answer = 0;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) {
        cin >> node[i];
    }
    for (int i = 0; i < m; i++) {
        int first, second;
        cin >> first >> second;
    }
}